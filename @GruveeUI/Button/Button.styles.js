import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    button: {
        borderRadius: 50,
        display: 'flex',
        flexBasis: 44,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100%',
        width: '100%',
    },
})
