import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    button: {
        borderRadius: 50,
        display: 'flex',
        flexBasis: 44,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
        width: '100%',
    },
})
